{
  "name": "lerna-test",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test",
    "scopedTest": "lerna run test --scope={@lerna-repo/common,@lerna-repo/fancy-case}",
    "new-version": "lerna version --conventional-commits --yes"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
